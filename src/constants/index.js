const ADMIN = 'admin';
const BLOGGER = 'blogger';
const GUEST = 'guest';
const USER = 'user';
const CREATE = 'create';

const CLOUDINARY_ROOT_DIR_NAME = 'blog';

module.exports = {
  ADMIN,
  BLOGGER,
  GUEST,
  USER,
  CREATE,
  CLOUDINARY_ROOT_DIR_NAME,
};